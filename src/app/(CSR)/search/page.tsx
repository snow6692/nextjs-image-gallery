import React from "react";
import SearchPage from "./SearchPage";

export const metadata = {
  title: "Search"
};

function page() {
  return (
    <div>
      <SearchPage />
    </div>
  );
}

export default page;
